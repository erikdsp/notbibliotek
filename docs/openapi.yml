openapi: 3.0.0
info:
  title: notbibliotek API
  version: 1.0.1
  description: organize sheet music for an orchestra
servers:
  - url: http://localhost:8080/api/v1
    description: Local Development Server
paths:
  /songs:
    get:
      summary: Get songs
      description: Returns songs in the library.
      operationId: get-songs
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Song'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: archived
          in: query
          required: false
          schema:
            type: boolean
        - name: search
          in: query
          required: false
          schema:
            type: string
      security:
        - libraryAccess: []
    post:
      summary: Post song
      description: Post a song to the library
      operationId: post-song
      responses:
        '201':
          description: Song created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Song'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSongRequest'
      security:
        - adminAuth: []
  /songs/{song_id}:
    patch:
      summary: Patch song
      operationId: patch-song
      responses:
        '200':
          description: Song updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Song'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Song not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: song_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSongRequest'
      security:
        - adminAuth: []
  /instruments:
    get:
      summary: Get instruments
      operationId: get-instruments
      responses:
        '200':
          description: Instruments defined in the library.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Instrument'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      security:
        - libraryAccess: []
    post:
      summary: Post instrument
      operationId: post-instrument
      responses:
        '201':
          description: Instrument created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Instrument'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateInstrumentRequest'
      security:
        - adminAuth: []
  /instruments/{instrument_id}:
    delete:
      summary: Delete instrument
      operationId: delete-instrument
      responses:
        '200':
          description: Instrument deleted.
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Instrument not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: instrument_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - adminAuth: []        
  /concerts:
    get:
      summary: Get concerts
      operationId: get-concerts
      responses:
        '200':
          description: Concerts in library.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Concert'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      security:
        - libraryAccess: []
    post:
      summary: Post concert
      operationId: post-concert
      responses:
        '201':
          description: Concert created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Concert'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateConcertRequest'
      security:
        - adminAuth: []
  /concerts/{concert_id}:
    get:
      summary: Get concert by id
      operationId: get-concert
      responses:
        '200':
          description: Concert.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Concert'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Concert not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: concert_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - libraryAccess: []
    patch:
      summary: Patch concert
      operationId: patch-concert
      responses:
        '200':
          description: Concert updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Concert'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Concert not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: concert_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateConcertRequest'
      security:
        - adminAuth: []
  /concerts/{concert_id}/songs:
    get:
      summary: Get concert songs
      operationId: get-concert-songs
      responses:
        '200':
          description: Concert songs.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Song'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Concert not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: concert_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - libraryAccess: []
    post:
      summary: Post song to concert
      operationId: post-song-to-concert
      responses:
        '201':
          description: Song created in concert.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Song'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Resource not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: concert_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddSongToConcertRequest'
      security:
        - adminAuth: []
  /concerts/{concert_id}/songs/{song_id}:
    delete:
      summary: Delete song from concert
      operationId: delete-song-from-concert
      responses:
        '200':
          description: Song deleted from concert.
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Resource not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: concert_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
        - name: song_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - adminAuth: []
  /concerts/{concert_id}/parts:
    get:
      summary: Get concert parts
      operationId: get-concert-parts
      responses:
        '200':
          description: Concert parts.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ConcertPartResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Concert not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: instrument_id
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ULID"
        - name: concert_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - libraryAccess: []
  /concerts/{concert_id}/scores:
    get:
      summary: Get concert scores
      operationId: get-concert-scores
      responses:
        '200':
          description: Concert Scores.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ConcertScoreResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Concert not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: concert_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - libraryAccess: []
  /parts/{part_id}/file:
    get:
      summary: Get part file
      operationId: get-part-file
      responses:
        '200':
          description: Part file.
          content:
            application/pdf:
              schema:
                $ref: '#/components/schemas/PdfContent'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Resource not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: part_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - libraryAccess: []
  /parts/{part_id}/instruments:
    post:
      summary: Post part instrument connection
      operationId: post-part-instrument
      responses:
        '201':
          description: Part Instrument created.
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Part not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: part_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePartInstrumentRequest'
      security:
        - adminAuth: []
  /parts/{part_id}/versions:
    get:
      summary: Get part versions
      operationId: get-part-versions
      responses:
        '200':
          description: Part versions.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PartVersion'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Part not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: part_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - libraryAccess: []
    post:
      summary: Post part version
      operationId: post-part-version
      responses:
        '201':
          description: Part Version created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PartVersion'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Part not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: part_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UploadPartVersionRequest'
      security:
        - adminAuth: []
  /songs/{song_id}/parts:
    post:
      summary: Post part
      operationId: post-part
      responses:
        '201':
          description: Song part created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Part'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: song_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePartRequest'
      security:
        - adminAuth: []
  /songs/{song_id}/score/file:
    get:
      summary: Get score file
      operationId: get-score-file
      responses:
        '200':
          description: Score file.
          content:
            application/pdf:
              schema:
                $ref: '#/components/schemas/PdfContent'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Song not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: song_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - libraryAccess: []
  /songs/{song_id}/score/versions:
    get:
      summary: Get score versions
      operationId: get-score-versions
      responses:
        '200':
          description: Score versions.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ScoreVersion'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Song not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: song_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - libraryAccess: []
    post:
      summary: Post score version
      operationId: post-score-version
      responses:
        '201':
          description: Score version created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScoreVersion'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Song not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: song_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UploadScoreVersionRequest'
      security:
        - adminAuth: []
  /parts/{part_id}/versions/{version_id}/publish:
    post:
      summary: Publish part version
      operationId: post-part-version-publish
      responses:
        '200':
          description: Part version published.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PartVersion'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Resource not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: part_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
        - name: version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - adminAuth: []
  /parts/{part_id}/instruments/{instrument_id}:
    delete:
      summary: Delete part instrument
      operationId: delete-part-instrument
      responses:
        '200':
          description: Part instrument deleted.
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Resource not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: part_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
        - name: instrument_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - adminAuth: []
  /parts/{part_id}/versions/{version_id}/file:
    get:
      summary: Get part version file
      operationId: get-part-version-file
      responses:
        '200':
          description: Part Version File.
          content:
            application/pdf:
              schema:
                $ref: '#/components/schemas/PdfContent'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Resource not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: part_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
        - name: version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - libraryAccess: []
  /songs/{song_id}/score/versions/{version_id}/publish:
    post:
      summary: Publish score version
      operationId: post-score-version-publish
      responses:
        '200':
          description: Score version published.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScoreVersion'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Resource not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: song_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
        - name: version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - adminAuth: []
  /songs/{song_id}/score/versions/{version_id}/file:
    get:
      summary: Get score version file
      operationId: get-score-version-file
      responses:
        '200':
          description: Score Version File.
          content:
            application/pdf:
              schema:
                $ref: '#/components/schemas/PdfContent'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Resource not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      parameters:
        - name: song_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
        - name: version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ULID"
      security:
        - libraryAccess: []
components:
  schemas:
    ULID:
      type: string
      minLength: 26
      maxLength: 26
    Song:
      type: object
      properties:
        id:
          $ref: "#/components/schemas/ULID"
        title:
          type: string
        archived_at:
          type: string
          format: date-time
          nullable: true
      required:
        - id
        - title
        - archived_at
    Score:
      type: object
      properties:
        id:
          $ref: "#/components/schemas/ULID"
        song_id:
          $ref: "#/components/schemas/ULID"
      required:
        - id
        - song_id
    Part: 
      type: object
      properties:
        id:
          $ref: "#/components/schemas/ULID"
        song_id:
          $ref: "#/components/schemas/ULID"
        name:
          type: string
      required:
        - id
        - song_id
        - name
    Instrument:
      type: object
      properties: 
        id:
          $ref: "#/components/schemas/ULID"
        name:
          type: string
      required:
        - id
        - name
    PartVersion:
      type: object
      properties:
        id:
          $ref: "#/components/schemas/ULID"
        part_id:
          $ref: "#/components/schemas/ULID"
        file_id:
          $ref: "#/components/schemas/ULID"
        published_at:
          type: string
          format: date-time
          nullable: true          
      required:
        - id
        - part_id
        - file_id
        - published_at        
    ScoreVersion:
      type: object
      properties:
        id:
          $ref: "#/components/schemas/ULID"
        score_id:
          $ref: "#/components/schemas/ULID"
        file_id:
          $ref: "#/components/schemas/ULID"
        published_at:
          type: string
          format: date-time
          nullable: true          
      required:
        - id
        - score_id
        - file_id
        - published_at
    File:
      type: object
      nullable: true
      properties:
        id:
          $ref: "#/components/schemas/ULID"
        file_name:
          type: string
      required:
        - id
        - file_name
    Concert:
      type: object
      properties:
        id:
          $ref: "#/components/schemas/ULID"
        name:
          type: string
        date:
          type: string
          format: date
      required:
        - id
        - name
        - date
    ConcertPartResponse:
      allOf:
        - $ref: "#/components/schemas/Part"
        - type: object
          properties:
            file:
              $ref: '#/components/schemas/File'
          required:
            - file
    ConcertScoreResponse:
      allOf:
        - $ref: "#/components/schemas/Score"
        - type: object
          properties:
            file:
              $ref: '#/components/schemas/File'
          required:
            - file
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    CreateSongRequest:
      type: object
      properties:
        title:
          type: string
      required:
        - title
    CreatePartInstrumentRequest:
      type: object
      properties:
        instrument_id:
          $ref: "#/components/schemas/ULID"
      required:
        - instrument_id
    CreateInstrumentRequest:
      type: object
      properties:
        name:
          type: string
      required:
        - name
    CreateConcertRequest:
      type: object
      properties:
        name:
          type: string
        date:
          type: string
          format: date
      required:
        - name
        - date
    UpdateConcertRequest:
      type: object
      properties:
        name:
          type: string
        date:
          type: string
          format: date
    AddSongToConcertRequest:
      type: object
      properties:
        song_id:
          $ref: "#/components/schemas/ULID"
      required:
        - song_id
    UpdateSongRequest:
      type: object
      properties:
        title:
          type: string
        archived:
          type: boolean
    CreatePartRequest:
      type: object
      properties:
        name:
          type: string
      required:
        - name
    UploadPartVersionRequest:
      type: object
      properties:
        file:
          type: string
          format: binary
      required:
        - file
    PdfContent:
      type: string
      format: binary
    UploadScoreVersionRequest:
      type: object
      properties:
        file:
          type: string
          format: binary
      required:
        - file
  securitySchemes:
    adminAuth:
      type: http
      description: Admin Authentication
      scheme: bearer
      bearerFormat: JWT
    libraryAccess:
      type: apiKey
      description: Link with library access
      name: access_token
      in: query
security: []
